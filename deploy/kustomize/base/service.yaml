apiVersion: v1
kind: Service
metadata:
  name: ksit-controller-manager-metrics-service
  namespace: ksit-system
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: ksit
    app.kubernetes.io/component: controller
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    control-plane: controller-manager